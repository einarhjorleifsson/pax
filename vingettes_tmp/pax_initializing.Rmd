# Seeding of the pax package

#### Copy the batch files used last year
```{r, eval=FALSE}
setwd("inst/shell_scripts")
system("cp -r -p /net/hafkaldi/export/u2/reikn/Tac/2013/01/02cn/pax/batch/* .")
```

Copy some makelist files
```{r, eval=FALSE}
system("cp /net/hafkaldi/export/u2/Stofnmat/make* .")
```

Commit to git





#### Copy the cod setup
Here just move the stuff so it is part of the package:
```{r, eval=FALSE}
setwd("~/r/Pakkar/pax/inst/setup")
system("cp -r -p /net/hafkaldi/export/home/haf/einarhj/src/Setup/agelen  .")
system("cp -r -p /net/hafkaldi/export/home/haf/einarhj/src/Setup/avelewt  .")
system("cp -r -p /net/hafkaldi/export/home/haf/einarhj/src/Setup/catch_no .")
system("cp -r -p /net/hafkaldi/export/home/haf/einarhj/src/Setup/keys .")
system("cp -r -p /net/hafkaldi/export/home/haf/einarhj/src/Setup/ldist  .")
```



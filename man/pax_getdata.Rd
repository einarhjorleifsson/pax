% Generated by roxygen2 (4.1.0): do not edit by hand
% Please edit documentation in R/pax_functions.R
\name{pax_getdata}
\alias{pax_getdata}
\title{Gets all relevant input data for generating catch in numbers}
\usage{
pax_getdata(pax = "shell", path)
}
\arguments{
\item{pax}{character, "shell" (default) calls "original" shell scripts.
If "R" (not implemented yet) runs native R scripts.}

\item{path}{The path to the data. Only works if pax="R". If missing (default)
the data is obtained from: /net/hafkaldi/export/u2/data.}
}
\description{
A wrapper around the (01)vpasksl.sh.

When pax="shell" user has to have access to the /net/hafkaldi/export/u2/data directory.
When pax="R" the user can specify the path to the data. The directory within
is assumed to be the same as the net-directory.

The following files are generated in the data directory
\itemize{
  \item sskyyyy.pre  otolith data
  \item sslyyyy.pre  length data
  \item ssskyyyy.pre
  \item ssslyyyy.pre
  \item k
  \item l
  \item stod
  \item t
}

In addition relevant control files (v, s and t files) are made in the keys
and ldist directory
}

